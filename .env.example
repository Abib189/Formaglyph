# Keep local for the zero-backend demonstration.
VITE_DATA_MODE=local

# Set these only when VITE_DATA_MODE=supabase. Use a publishable key, never a
# secret key or service-role key.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=replace-with-local-or-hosted-publishable-key
